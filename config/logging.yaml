version: 1
formatters:
  standard:
    format: "{time:YYYY-MM-DD HH:mm:ss} | {level} | {name} | {message}"
handlers:
  console:
    sink: sys.stdout
    level: INFO
  file:
    sink: logs/quantzai_{time:YYYY-MM-DD}.log
    rotation: "1 day"
    retention: "30 days"
    level: DEBUG
